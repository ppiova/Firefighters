@model Firefighters.Web.Models.ElementoViewModel

@{
    ViewData["Title"] = "Editar";
}

<h4>Edición de Elemento</h4>
<hr />
<form asp-action="Edit">

    <div asp-validation-summary="ModelOnly" class="text-danger"></div>
    <input type="hidden" asp-for="ElementoID" />

    <partial name="_Elemento" />

    @*@if (Model.Activo == false)
        {  <div class="form-group">
                <div class="checkbox">
                    <label>
                        <input asp-for="Activo" /> @Html.DisplayNameFor(model => model.Activo)
                    </label>
                </div>
            </div>}*@
    @if (Model.BajaFecha != null)
    {<div class="form-row">
            <div class="form-group col-md-4">
                <label asp-for="BajaFecha" class="control-label"></label>
                <input asp-for="BajaFecha" class="form-control" />
                <span asp-validation-for="BajaFecha" class="text-danger"></span>
            </div>
        </div>}

    <div class="form-group">
        <input type="submit" value="Guardar" class="btn btn-primary btn-user btn-block" /> |
        <input type="submit" asp-action="BajaLogica" asp-route-id="@Model.ElementoID" value="NO Activo" class="btn small btn-danger " />
    </div>
    <div>
        <a asp-action="Index" class="btn btn-secondary btn-block"><i class="fas fa-long-arrow-alt-left"></i>&nbsp;Volver Listado</a>
    </div>
</form>




@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
